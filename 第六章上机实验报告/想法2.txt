输入函数

处理输入并存储被积函数形式

输入上下限

判断函数在区间内是否可积（被积函数不能发散）

求插值节点值

带入不同公式求值

模块：
GUI
函数输入模块
输入处理与存储模块
函数可积性判断模块
求积分模块，求积分函数库——核心部分

求积分函数
一元函数形式存储对象：运算符：+、-、*、^、sin、cos、tan、log，常数：e、π
二元函数？

求积分函数传入函数存储对象，积分上限，积分下限

实时生成标记序列，窗口显示相应字符串。光标点击到一个标记内或一个标记后时，按退格键，删除的是整个标记

只允许通过点击按钮来输入标记。键盘的部分按键绑定到按钮上



